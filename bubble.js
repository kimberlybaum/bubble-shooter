window.onload = function() {

    let bubbleRadius = 20;
    let bubbleColors = ["pink","blue", "purple", "hotpink"];

    class Bubble {
        constructor(x, y, c) {
            this.x = x;
            this.y = y;
            this.color = c;
        }
    }

    
}
